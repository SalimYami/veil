[veil_servers]
localhost ansible_connection=local ansible_become_password="{{ lookup('env', 'ANSIBLE_SUDO_PASS') }}"

# For remote VPS deployment (uncomment and configure):
# my-vps ansible_host=YOUR_SERVER_IP ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/id_rsa

[veil_servers:vars]
ansible_python_interpreter=/usr/bin/python3
