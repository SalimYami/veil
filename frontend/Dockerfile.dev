FROM node:22-alpine

WORKDIR /app

# Copy package files (lock files excluded by .dockerignore, install fresh)
COPY package.json ./

# Install dependencies
RUN npm install

# Source will be mounted via volume (hot reload)
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
